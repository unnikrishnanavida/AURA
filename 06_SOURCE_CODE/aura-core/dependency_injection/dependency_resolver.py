from __future__ import annotations

from typing import Dict


class DependencyResolver:
    def resolve(self, descriptors: Dict[str, str]) -> Dict[str, str]:
        # Placeholder: in richer DI systems this orders and resolves dependencies.
        return descriptors
